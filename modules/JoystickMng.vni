package pilot.base;

module JoystickMng {
	
	vnx.TopicPtr output;						// pilot.JoystickData
	
	bool invert = false;						// if to invert axes
	
	int read_timeout_ms = 200;					// read timeout [ms]
	
	int error_interval_ms = 1000;				// retry interval for opening device [ms]
	
	int stats_interval_ms = 10000;				// status message interval (0 = disable) [ms]
	
}
